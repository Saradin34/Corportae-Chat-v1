{
  "name": "corporate-chat-tauri",
  "version": "2.0.0",
  "private": true,
  "description": "Corporate Chat lightweight desktop wrapper based on Tauri/WebView2",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:win": "tauri build --bundles nsis",
    "build:msi": "tauri build --bundles msi",
    "build:portable": "tauri build --bundles nsis"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
